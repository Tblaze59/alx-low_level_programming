#include "main.h"
/**
 * factorial - factorial of a number
 * @n: number whose factorial is being found
 * Return: factorial of n
 */
int factorial(int n)
{
	int factorial = n;

	if (n < 0)
		return (-1);

	else if (n >= 0 && n <= 1)
		return (1);
}
